import 'package:flutter/material.dart';

class PrivateSessions extends StatelessWidget {
  const PrivateSessions({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
