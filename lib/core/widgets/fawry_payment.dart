import 'package:flutter/material.dart';

class FawryPaymentWidget extends StatelessWidget {
  const FawryPaymentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fawry Credit Selected'),
    );
  }
}
